services:
  # Frontend Service
  - type: web
    name: revnet3-frontend
    env: node
    plan: free
    branch: main
    autoDeploy: true
    buildCommand: npm ci --only=production && npm run build:prod
    startCommand: npx serve -s dist/revnet -l 3000
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000

  # Backend Service
  - type: web
    name: revnet3-backend
    env: node
    plan: free
    branch: main
    autoDeploy: true
    buildCommand: cd backend && npm ci && npx nest build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        fromDatabase:
          name: revnet3-db
          property: connectionString
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_PUBLISHABLE_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: FRONTEND_URL
        value: https://revnet3-frontend.onrender.com

databases:
  - name: revnet3-db
    plan: free
    databaseName: revnet3
    user: revnet3_user
